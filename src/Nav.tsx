import{Home,Map,Gift,BarChart3}from'lucide-react';export default function Nav({page,setPage}:{page:string;setPage:(p:string)=>void}){return <nav className="nav">{[['home',Home,'Home'],['journey',Map,'Learn'],['rewards',Gift,'Rewards'],['progress',BarChart3,'More']].map(([id,Icon,label]:any)=><button key={id} className={page===id?'active':''} onClick={()=>setPage(id)}><Icon size={22}/><span>{label}</span></button>)}</nav>}
